# GENERATED by bench/gen_llamaswap_config.py -- do not hand-edit.
# from bench-results.json 2026-09-09T18:25:53-0700 (llama.cpp 2d8d612e4, force_mmq=True)

healthCheckTimeout: 480
logLevel: info
startPort: 10800

macros:
  server: "/home/you/.local/bin/llama-server"

models:
  "coder-1.5b-fim":
    # Coder Coder 1.5B (FIM / infill only) | 109.9 tok/s | 1.51s cold | 1999 MiB VRAM
    cmd: |
      ${server}
      -m /home/you/models/coder-1.5b-fim.gguf
      --host 127.0.0.1
      --port ${PORT}
      -c 8192
      -ngl 99
      -fa on
      -t 6
      -np 1
      --no-webui
      --metrics
    proxy: "http://127.0.0.1:${PORT}"
    checkEndpoint: "/health"
    ttl: 1200
    unlisted: true

  "fast-4b":
    # FamilyA 4B (Q8_0) | 50.0 tok/s | 2.74s cold | 7125 MiB VRAM
    cmd: |
      ${server}
      -m /home/you/models/fast-4b.gguf
      --host 127.0.0.1
      --port ${PORT}
      -c 131072
      -ngl 99
      -fa on
      -t 6
      -np 1
      --no-webui
      --metrics
      -ctk q8_0
      -ctv q8_0
      --temp 0.6
      --top-p 0.95
      --top-k 20
      --min-p 0
      -b 2048
      -ub 512
    proxy: "http://127.0.0.1:${PORT}"
    checkEndpoint: "/health"
    ttl: 1200
    filters:
      setParams:
        chat_template_kwargs:
          enable_thinking: false

  "daily-9b":
    # FamilyA 9B (Q6_K) | 32.9 tok/s | 3.86s cold | 8377 MiB VRAM
    cmd: |
      ${server}
      -m /home/you/models/daily-9b.gguf
      --host 127.0.0.1
      --port ${PORT}
      -c 98304
      -ngl 99
      -fa on
      -t 6
      -np 1
      --no-webui
      --metrics
      -ctk q8_0
      -ctv q8_0
      --temp 0.6
      --top-p 0.95
      --top-k 20
      --min-p 0
      -b 2048
      -ub 512
    proxy: "http://127.0.0.1:${PORT}"
    checkEndpoint: "/health"
    ttl: 1200
    filters:
      setParams:
        chat_template_kwargs:
          enable_thinking: false

  "alt-4b":
    # FamilyB 4B (alt finetune, Q8_0) | 43.8 tok/s | 4.51s cold | 6057 MiB VRAM
    cmd: |
      ${server}
      -m /home/you/models/alt-4b.gguf
      --host 127.0.0.1
      --port ${PORT}
      -c 65536
      -ngl 99
      -fa on
      -t 6
      -np 1
      --no-webui
      --metrics
      -ctk q8_0
      -ctv q8_0
      --chat-template-file /home/you/models/alt-4b.chat_template.jinja
    proxy: "http://127.0.0.1:${PORT}"
    checkEndpoint: "/health"
    ttl: 1200

  "moe-26b":
    # FamilyB 26B QAT (Q4_0, MoE) | 34.8 tok/s | 11.91s cold | 6787 MiB VRAM
    cmd: |
      ${server}
      -m /home/you/models/moe-26b.gguf
      --host 127.0.0.1
      --port ${PORT}
      -c 24576
      -ngl 99
      -fa on
      -t 6
      -np 1
      --no-webui
      --metrics
      --n-cpu-moe 20
      -ctk q8_0
      -ctv q8_0
      --no-mmap
      -b 2048
      -ub 512
    proxy: "http://127.0.0.1:${PORT}"
    checkEndpoint: "/health"
    ttl: 1200
    filters:
      setParams:
        chat_template_kwargs:
          enable_thinking: false

  "moe-30b":
    # FamilyC Flash (Q4_K_M, MoE) | 29.4 tok/s | 16.87s cold | 8063 MiB VRAM
    cmd: |
      ${server}
      -m /home/you/models/moe-30b.gguf
      --host 127.0.0.1
      --port ${PORT}
      -c 24576
      -ngl 99
      -fa on
      -t 6
      -np 1
      --no-webui
      --metrics
      --n-cpu-moe 32
      -ctk q8_0
      -ctv q8_0
      --no-mmap
      --temp 1.0
      --top-p 0.95
    proxy: "http://127.0.0.1:${PORT}"
    checkEndpoint: "/health"
    ttl: 1200
    filters:
      setParams:
        chat_template_kwargs:
          enable_thinking: false

  "moe-35b":
    # FamilyA 35B (Q4_K_M, MoE) | 32.0 tok/s | 22.95s cold | 7155 MiB VRAM
    cmd: |
      ${server}
      -m /home/you/models/moe-35b.gguf
      --host 127.0.0.1
      --port ${PORT}
      -c 49152
      -ngl 99
      -fa on
      -t 6
      -np 1
      --no-webui
      --metrics
      --n-cpu-moe 32
      -ctk q8_0
      -ctv q8_0
      --no-mmap
      --temp 0.6
      --top-p 0.95
      --top-k 20
      --min-p 0
      -b 2048
      -ub 512
    proxy: "http://127.0.0.1:${PORT}"
    checkEndpoint: "/health"
    ttl: 1200
    filters:
      setParams:
        chat_template_kwargs:
          enable_thinking: false

  "moe-35b-vision":
    # FamilyA 35B (Q4_K_M, MoE) -- vision | mmproj: mmproj-F16.gguf
    cmd: |
      ${server}
      -m /home/you/models/moe-35b.gguf
      --host 127.0.0.1
      --port ${PORT}
      -c 49152
      -ngl 99
      -fa on
      -t 6
      -np 1
      --no-webui
      --metrics
      --mmproj /home/you/models/moe-35b.mmproj.gguf
      --n-cpu-moe 32
      -ctk q8_0
      -ctv q8_0
      --no-mmap
    proxy: "http://127.0.0.1:${PORT}"
    checkEndpoint: "/health"
    ttl: 1200
    filters:
      setParams:
        chat_template_kwargs:
          enable_thinking: false
    unlisted: true

  "moe-26b-vision":
    # FamilyB 26B QAT (Q4_0, MoE) -- vision | mmproj: moe-26b.gguf
    cmd: |
      ${server}
      -m /home/you/models/moe-26b.gguf
      --host 127.0.0.1
      --port ${PORT}
      -c 24576
      -ngl 99
      -fa on
      -t 6
      -np 1
      --no-webui
      --metrics
      --mmproj /home/you/models/moe-26b.mmproj.gguf
      --n-cpu-moe 20
      -ctk q8_0
      -ctv q8_0
      --no-mmap
    proxy: "http://127.0.0.1:${PORT}"
    checkEndpoint: "/health"
    ttl: 1200
    filters:
      setParams:
        chat_template_kwargs:
          enable_thinking: false
    unlisted: true

groups:
  # v1: SMALL_MODEL_FAST_LANE off -> app serialises everything, one
  # model loaded at a time. Groups make that explicit + ready the fast lane.
  "heavyweights":
    swap: true
    exclusive: true
    members: ["moe-26b", "moe-30b", "moe-35b", "moe-26b-vision", "moe-35b-vision"]
  "standard":
    swap: true
    exclusive: true
    members: ["fast-4b", "daily-9b", "alt-4b"]
  "fim":
    swap: true
    exclusive: true
    members: ["coder-1.5b-fim"]
