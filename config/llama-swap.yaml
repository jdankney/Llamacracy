# GENERATED by bench/gen_llamaswap_config.py -- do not hand-edit.
# from bench-results.json 2026-09-09T18:25:53-0700 (llama.cpp 2d8d612e4, force_mmq=True)

healthCheckTimeout: 480
logLevel: info
startPort: 10800

macros:
  server: "/home/you/.local/bin/llama-server"

models:
  "coder-1.5b-fim":
    # Coder Coder 1.5B (FIM / infill only) | 109.9 tok/s | 1.51s cold | 1999 MiB VRAM
    cmd: |
      ${server}
      -m /home/you/models/coder-1.5b-fim.gguf
      --host 127.0.0.1
      --port ${PORT}
      -c 8192
      -ngl 99
      -fa on
      -t 6
      -np 1
      --no-webui
      --metrics
    proxy: "http://127.0.0.1:${PORT}"
    checkEndpoint: "/health"
    ttl: 1200
    unlisted: true

  "fast-4b":
    # FamilyA 4B (Q8_0) | 44.3 tok/s | 2.8s cold | 5155 MiB VRAM
    cmd: |
      ${server}
      -m /home/you/models/fast-4b.gguf
      --host 127.0.0.1
      --port ${PORT}
      -c 32768
      -ngl 99
      -fa on
      -t 6
      -np 1
      --no-webui
      --metrics
      -ctk q8_0
      -ctv q8_0
      --temp 0.6
      --top-p 0.95
      --top-k 20
      --min-p 0
      -b 2048
      -ub 512
    proxy: "http://127.0.0.1:${PORT}"
    checkEndpoint: "/health"
    ttl: 1200
    filters:
      setParams:
        chat_template_kwargs:
          enable_thinking: false

  "daily-9b":
    # FamilyA 9B (Q6_K) | 30.4 tok/s | 3.9s cold | 7125 MiB VRAM
    cmd: |
      ${server}
      -m /home/you/models/daily-9b.gguf
      --host 127.0.0.1
      --port ${PORT}
      -c 32768
      -ngl 99
      -fa on
      -t 6
      -np 1
      --no-webui
      --metrics
      -ctk q8_0
      -ctv q8_0
      --temp 0.6
      --top-p 0.95
      --top-k 20
      --min-p 0
      -b 2048
      -ub 512
    proxy: "http://127.0.0.1:${PORT}"
    checkEndpoint: "/health"
    ttl: 1200
    filters:
      setParams:
        chat_template_kwargs:
          enable_thinking: false

  "alt-4b":
    # FamilyB 4B (alt finetune, Q8_0) | 39.8 tok/s | 4.51s cold | 5641 MiB VRAM
    cmd: |
      ${server}
      -m /home/you/models/alt-4b.gguf
      --host 127.0.0.1
      --port ${PORT}
      -c 32768
      -ngl 99
      -fa on
      -t 6
      -np 1
      --no-webui
      --metrics
      --chat-template-file /home/you/models/alt-4b.chat_template.jinja
    proxy: "http://127.0.0.1:${PORT}"
    checkEndpoint: "/health"
    ttl: 1200

  "moe-26b":
    # FamilyB 26B QAT (Q4_0, MoE) | 33.8 tok/s | 12.19s cold | 7556 MiB VRAM
    cmd: |
      ${server}
      -m /home/you/models/moe-26b.gguf
      --host 127.0.0.1
      --port ${PORT}
      -c 16384
      -ngl 99
      -fa on
      -t 6
      -np 1
      --no-webui
      --metrics
      --n-cpu-moe 18
      --no-mmap
      -b 2048
      -ub 512
    proxy: "http://127.0.0.1:${PORT}"
    checkEndpoint: "/health"
    ttl: 1200

  "moe-30b":
    # FamilyC Flash (Q4_K_M, MoE) | 29.1 tok/s | 13.56s cold | 9138 MiB VRAM
    cmd: |
      ${server}
      -m /home/you/models/moe-30b.gguf
      --host 127.0.0.1
      --port ${PORT}
      -c 16384
      -ngl 99
      -fa on
      -t 6
      -np 1
      --no-webui
      --metrics
      --n-cpu-moe 30
      --no-mmap
      --temp 1.0
      --top-p 0.95
    proxy: "http://127.0.0.1:${PORT}"
    checkEndpoint: "/health"
    ttl: 1200
    filters:
      setParams:
        chat_template_kwargs:
          enable_thinking: false

  "moe-35b":
    # FamilyA 35B (Q4_K_M, MoE) | 31.8 tok/s | 25.36s cold | 8474 MiB VRAM
    cmd: |
      ${server}
      -m /home/you/models/moe-35b.gguf
      --host 127.0.0.1
      --port ${PORT}
      -c 16384
      -ngl 99
      -fa on
      -t 6
      -np 1
      --no-webui
      --metrics
      --n-cpu-moe 28
      --no-mmap
      --temp 0.6
      --top-p 0.95
      --top-k 20
      --min-p 0
      -b 2048
      -ub 512
    proxy: "http://127.0.0.1:${PORT}"
    checkEndpoint: "/health"
    ttl: 1200
    filters:
      setParams:
        chat_template_kwargs:
          enable_thinking: false

groups:
  # v1: SMALL_MODEL_FAST_LANE off -> app serialises everything, one
  # model loaded at a time. Groups make that explicit + ready the fast lane.
  "heavyweights":
    swap: true
    exclusive: true
    members: ["moe-26b", "moe-30b", "moe-35b"]
  "standard":
    swap: true
    exclusive: true
    members: ["fast-4b", "daily-9b", "alt-4b"]
  "fim":
    swap: true
    exclusive: true
    members: ["coder-1.5b-fim"]
